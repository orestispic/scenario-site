name = "scenario-commercial-api-local-test"
main = "worker/src/local-test.ts"
compatibility_date = "2026-05-22"
compatibility_flags = ["nodejs_compat"]

# This entry point owns only deterministic local identities. It imports no
# Supabase service-role secret and is rejected by the production entry point.
